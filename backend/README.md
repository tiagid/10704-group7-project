# Backend

Python 3.12 transcription pipeline and FastAPI service. Owned by Brady.

Planned modules: audio loading (librosa), pitch tracking (crepe for Phase 1, basic-pitch for Phase 2), MIDI export (mido / pretty_midi), and a FastAPI app exposing `POST /transcribe`.

Run tests with `pytest` from this directory.
